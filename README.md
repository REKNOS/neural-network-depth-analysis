# neural-network-depth-analysis
This project explores how the depth of a neural network (i.e., the number of hidden layers) affects its ability to learn complex patterns.  We compare multiple neural network architectures with different depths and analyse their performance using a nonlinear classification dataset.
